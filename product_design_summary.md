# Situational Awareness · 产品设计 Summary

**一句话定位**：面向在美中国职场人的邀请制社群，把"一个人闷头想不通的职场困境"变成"一群人帮你想"，同时沉淀成可复用的判断力。

**目标用户**
- Mid-career，想成为 decision maker、拓展 social network 的人
- 缺少 mentorship / sponsorship 的中国职场人
- 想找到更多导师和支持者，但不擅长在大群里主动社交的"i人"

**核心场景**（用户会带着这些问题来）
- 遇到一个进退两难的具体决策，需要策略建议
- 想更深入理解一个场景，或获得更高维度的认知视角
- 和上级沟通出现问题、团队内部意见冲突，需要第三方视角
- 想找人 mock 一次重要 presentation，顺便让听众也学到东西

---

## 当前进度（persisted here so the team can check status anytime — update as PRs merge）

线上：https://helenakapeach.github.io/situational_awareness/（`main`，Vite + 原生 JS + Supabase，见第 4 节）

旧演示站 https://situational-awareness-mmmvp.luohongchen1993.chatgpt.site/ 不再作为进度依据。

**已上线**（讨论闭环已经齐，不再按功能清单往下堆）
- Google 登录 + 邀请码准入（兑换大小写归一、并发兑换安全；码的创建/停用仍走 Supabase SQL，无管理后台）
- 发帖始终匿名：`posts.author_id` 对 Data API 无 SELECT，不是前端隐藏
- 回复默认真名（Google 显示名），可选匿名；匿名回复由库层剥掉身份
- 回复点赞（一人一赞可取消），帖内按赞数降序、同分按时间升序
- 帖子和回复的轻量 Markdown（去 HTML，图片降级为 alt，链接仅 http/https/mailto）
- 登录后 topbar 反馈通道（客户端只能 insert，团队在 SQL 里读）
- 顶栏导航外壳：讨论广场 / 案例库 / 圈子 / 饭局 四个 tab，右上角私信 / 我，外加对已取回帖子的客户端搜索和深浅色切换。**只有讨论广场是真的**——其余四个是空状态占位，没有建表、没有后端、没有路由，不要当成已完成的模块

**下一步（按这个顺序，不要并行铺开）**
1. **冷启动**：确认线上 migration 已跟上 `main`，用 SQL 建好邀请码；核心成员先发 3–5 个真实困境帖；邀请 50 人以内
2. **极简邮件通知**：只在冷启动证明「回了没人知道」之后再做。邮件不写标题、摘录、回复者、帖子 ID，只说「你有新回复」，链到首页。站内铃铛、设置表、digest worker 一律不做
3. **实名回复上的角色标签**（导师 / 学员 / 其他）：只挂在实名回复上，匿名发帖和匿名回复都不带标签。不要顺手做完整主页

**现在不要做**
- 分类（帖少时会把 feed 拆空）
- 完整注册表 / 主页 / 三项联系方式开关 / 私密联系分享（和角色标签是一簇，等有稳定的实名回答再做）
- 知识库（UI 里的「案例库」tab）、排行榜、徽章、Weekly 饭局（UI 里的「饭局」tab）——顶栏有入口只是为了把未来的形状先摆出来，点进去是空状态，不要因为「壳已经在了」就顺手填内容
- 圈子（按行业 / 届别 / 城市分组）：和「分类」是同一个风险，帖子还少的时候拆出来只会把每个圈子都拆空。等广场自己热起来再说
- 管理后台、邀请码限流（已知缺口，码还没对外扩散）
- 完整通知中心（铃铛 + 未读 + 设置 + Resend worker + cron）

---

## 1. 功能范围

### 已经做成的最小产品

邀请码自助准入，不设人工审核队列。登录邮箱来自 Google，不做十字段注册表。讨论是一条时间线：发帖始终匿名，回复默认真名、可选匿名，高赞回答浮到上面。管理动作（建码、读反馈）走 Supabase SQL。

### 还想做成、但排在冷启动之后的

| 模块 | 说明 | 何时做 |
|---|---|---|
| **极简邮件通知** | 帖子收到新回复时通知楼主。人少时不能指望大家刷站。第一版必须很淡，避免 Resend 日志把邮箱和匿名帖绑死 | 冷启动证明回访断层之后 |
| **角色标签** | 四类角色自选：领航执委会 / 导师 / 领航学员（届别）/ 其他用户。未认证是默认态，**不用警示色**；已认证才用色板里的青玉。只出现在实名回复上 | 有稳定的实名回答、需要被信任时 |
| **主页与联系方式** | 公司/职业、学校、毕业年份、自我介绍；微信 / 公开邮箱 / LinkedIn 三项独立可见性开关 | 角色标签之后，不要提前 |
| **私密联系方式分享** | 某条回复里附上仅提问人可见的联系方式，RLS 强制 | 和主页同一簇，不要拆成独立优先项 |
| **知识库** | 版主加精 + 合作机构案例，管理员手录，按标签筛选 | 有可沉淀的高赞讨论之后 |
| **排行榜与徽章** | 积分流水 → 周/月/年榜；简单阈值徽章；年度冰箱贴 | 有回头客和贡献行为之后 |
| **Weekly 饭局** | 不自建，外链 Luma | 社区开始见面时，一个链接即可 |

**明确不做（现在和可预见的 V1 都砍）**
- 站内私信 / 群聊（有微信，不重复造轮子）。顶栏那个私信图标点进去就是说明「这里更适合公开讨论，要深聊用微信」——**它和上面「还没做」的四个壳性质不同，是一条不打算兑现的承诺**，留着还是撤掉需要单独定
- 图片、视频等富媒体
- 已读回执、实时输入中提示、通知铃铛的第一刀
- 自动化知识库摘要
- 复杂徽章规则引擎
- 发帖分类、完整注册表、管理后台——在帖子和成员都还少的时候做，只会增加表面

曾经写过「V1 明确不做匿名回复」。已经改口：回复可选匿名，这是产品决策，不是事故。

---

## 2. 关键设计原则

1. **发帖始终匿名；回复默认真名、可选匿名**：帖子没有实名路径。回复默认带 Google 显示名，勾选后由数据库剥成「匿名成员」。匿名楼主若要在自己帖下追加说明，既可以实名回复，也可以继续匿名——不再强迫露脸。
2. **匿名内容不可反查身份**：匿名帖和匿名回复的头像、昵称不能链到真实主页，也不展示身份标签。无论作者是否开放了联系方式，页面和 Data API 都看不到 `author_id`。
3. **身份标签是"结构性角色"，徽章是"行为战绩"，两者分开**：领航执委会 / 导师 / 领航学员（届别）/ 其他用户是身份标签；排行榜徽章是靠平台行为攒出来的。两套互不影响。第一刀只做标签，不做徽章。
4. **先自报、后认证，不阻塞加入**：标签由用户自己选，不做前置审核；未认证是最常见的默认状态，**不用警示色标记**（未认证 ≠ 异常/风险）。只有翻转为"已认证"后才用色板里的青玉做低调区分。管理员事后抽查翻转。
5. **联系方式可见性是独立开关**，不跟匿名状态强绑定。三项（微信 / 公开邮箱 / LinkedIn）各自开关。等做到主页再落地，现在不要为此建表。
6. **主页公开联系方式 ≠ 单条回复的私密联系方式分享**：前者面向全体成员，后者只向那一位提问人开放。两者都后置。
7. **邮件通知是早期留存的关键钩子**：社区人少时不能指望大家养成刷站习惯。下一刀如果做通知，先邮件、不做铃铛。给匿名楼主的邮件不带标题、摘录、回复者名字和帖子 ID，只链到首页——否则第三方邮件日志会破坏匿名。
8. **内容形态刻意从简**：文本 + 轻量 markdown，不做图片/视频。一条时间线，先不分分类。

---

## 3. 数据模型

### 已经存在

| 表 | 关键字段 |
|---|---|
| `invite_codes` | code（存大写）, created_by, is_active, max_uses（可选）, used_count |
| `members` | 邀请码兑换后写入；帖子/回复的 RLS 要求既是 Google 登录又是 member |
| `posts` | author_id（客户端不可读）, title, body。没有 category / is_anonymous / 帖级点赞——发帖在产品上就是匿名 |
| `replies` | post_id, author_id（客户端不可读）, body, author_name, author_avatar_url, is_anonymous, upvote_count |
| `reply_votes` | user_id, reply_id；配合 `toggle_reply_vote()` |
| `feedback` | author_id（客户端不可读）, body, created_at；只允许 insert |

### 以后才建（不要提前）

| 表 | 用途 |
|---|---|
| 资料字段（可挂在 `members` 上，不必另起 `users`） | role_type, cohort_year, role_verified；更后才是公司/学校/联系方式 |
| `notifications` | 极简邮件用：收件人、reply 类型、投递状态。不要为铃铛先建已读/设置表 |
| `knowledge_base_articles` / `points_ledger` / `badges` | 有内容和回头客之后 |

---

## 4. 技术栈

- **Vite + 原生 JS**（`index.html` / `src/app.js` / `src/backend.js`），不引入前端框架
- **design-tokens.css**：色板令牌唯一来源，见 `brand-style-sample.html`（详见 `CLAUDE.md`）
- **Supabase**：Postgres + Auth（Google OAuth）+ RLS；本地无变量或 `?demo=1` 时走 localStorage demo
- **Resend**：规划中，只在做邮件通知时接入
- **GitHub Pages**（`.github/workflows/deploy-pages.yml`）
- **Luma**：以后饭局用，现在不接

---

## 5. 还剩多少工期

已经做完的（搭建、邀请码、讨论广场、点赞、markdown、反馈）不再计入。

| 模块 | 预估 | 备注 |
|---|---|---|
| 冷启动（建码、发真帖、小范围邀请） | 运营，不是工程 | 本周就该做 |
| 极简邮件通知 | 1–2 天 | 触发器 + 投递；不做铃铛 |
| 实名回复上的角色标签 | 1–2 天 | 自报即可，认证翻转可更晚 |
| 主页 / 私密联系 / 知识库 / 榜单 / 饭局 | 未估 | 明确后置，不要按旧的 4.5–5.5 周总表排期 |

---

## 6. 成本估算

### 开发成本（一次性）

- **团队自建（用业余时间）**：现金成本约为 $0。讨论闭环已落地，剩下主要是冷启动时间和（可能的）邮件通知。
- 旧的 22–35 天 / $8,800–$22,400 外包估算对应的是整包 V1，不再适用。

### 运营成本（每月，100–600 人规模）

| 项目 | 起步阶段（免费额度内） | 稳定运营后建议 |
|---|---|---|
| GitHub Pages | $0 | $0 |
| Supabase 数据库 | $0（Free tier，500MB） | $25/月（Pro，含备份、不休眠） |
| Resend 邮件 | $0（3,000 封/月内；接入后才产生） | $20/月（用量上涨后） |
| Luma 活动 | 未接入 | 基础免费；可选 $59/月 |
| 域名 | — | 约 $12–15/年 |
| **合计** | **约 $0/月** | **约 $25–45/月**（未上 Luma；上了邮件大约再 +$20） |

纯文本/轻量 markdown，600 人规模下存储可忽略。

### 其他运营性成本
- 年度冰箱贴徽章：定制 + 邮寄，预估 **$3–5/枚**（按领取人数，不是全员）。榜单没做之前不必备货。

---

## 7. 建议节奏

1. **先冷启动**：核心成员发 3–5 个真实案例帖，邀请 50 人以内。看有没有人愿意匿名提问、有没有人认真回答、楼主会不会回来看。
2. **闭环若断在回访**：再做极简邮件通知（见「下一步」第 2 条）。不要先做铃铛。
3. **回复者若需要被信任**：再做实名回复上的角色标签。不要顺手做主页和联系方式。
4. **验证后再扩大邀请范围**。知识库、饭局、认证抽查、榜单都等有内容、有回头客再开。
